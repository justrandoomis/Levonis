diff --git a/node_modules/three-slicer/engine/src/slicer.worker.js b/node_modules/three-slicer/engine/src/slicer.worker.js
index 0aaf626..994863b 100644
--- a/node_modules/three-slicer/engine/src/slicer.worker.js
+++ b/node_modules/three-slicer/engine/src/slicer.worker.js
@@ -20,10 +20,37 @@ import { assertLegacySlaFallback, parseSlaJob } from './sla_request.js'
 let quiet = false
 const say = (level, ...args) => { if (!quiet) console[level](...args) }
 
+// LEVONIS PATCH — cap the pthread pool before the threaded core sizes it.
+// See patches/README.md. The core does, verbatim:
+//   pthreadPoolSize = navigator.hardwareConcurrency || 4
+//   while (pthreadPoolSize--) PThread.allocateUnusedWorker()
+// and each of those is a module Worker that parses this package's 5.28 MB
+// core, as a blocking addOnPreRun dependency. On an 8-core phone that is 8
+// extra isolates and 8 parses before the kernel reports ready, which is what
+// kills it. Shadowing the value on the worker's OWN navigator is enough:
+// initMainThread() reads it from here, in this scope, and the pool workers it
+// then spawns do not re-run that branch.
+const capPthreadPool = () => {
+  try {
+    const cores = navigator.hardwareConcurrency || 4
+    // deviceMemory is Chromium-only and is the reliable signal where it exists;
+    // the user-agent test covers the rest. A phone gets threads, just not one
+    // per core — that keeps SharedArrayBuffer, and with it slice cancellation
+    // and live support progress, which the single-threaded core cannot do.
+    const small = navigator.deviceMemory > 0 && navigator.deviceMemory <= 4
+    const handheld = /Android|iPhone|iPad|iPod/i.test(navigator.userAgent || '')
+    const cap = small || handheld ? 2 : cores
+    if (cap >= cores) return
+    Object.defineProperty(navigator, 'hardwareConcurrency', { value: cap, configurable: true })
+    say('info', '[slicer.worker] pthread pool capped at ' + cap + ' (device reports ' + cores + ')')
+  } catch { /* a navigator we cannot shadow keeps the engine's own default */ }
+}
+
 const loadCore = async () => {
   const isolated = typeof crossOriginIsolated !== 'undefined' && crossOriginIsolated
   if (isolated) {
     try {
+      capPthreadPool()
       const M = await (await import('./slicer_core.mt.js')).default()
       say('info', '[slicer.worker] core: mt (threads)')
       return M
diff --git a/node_modules/three-slicer/viewer/dist/Viewport.js b/node_modules/three-slicer/viewer/dist/Viewport.js
index 679c796..3119c1e 100644
--- a/node_modules/three-slicer/viewer/dist/Viewport.js
+++ b/node_modules/three-slicer/viewer/dist/Viewport.js
@@ -1660,7 +1660,7 @@ function _l(e) {
         } else
           E(ne), v(!1);
       };
-      k.onerror = (ne) => F("Worker terminated (likely out of memory): " + (ne.message || "worker error")), k.onmessageerror = () => F("Worker message error (structured clone failed)"), r.current = k, typeof window < "u" && (window.__vpWorker = k);
+      k.onerror = (ne) => F("Worker terminated (likely out of memory): " + (ne.message || "worker error")), k.onmessageerror = () => F("Worker message error (structured clone failed)"), r.current = k, typeof window < "u" && (window.__vpWorker = k, window.__vpReleaseWorker = () => { if ($.current || r.current !== k) return !1; try { k.terminate(); } catch { } return r.current = null, N.current = null, window.__vpWorker === k && (window.__vpWorker = null), !0; }, window.__vpCanCancel = () => !!(N.current && N.current.cancel), window.__vpAbortSlice = () => { const V = $.current; if (!V || r.current !== k) return !1; $.current = null; try { V.stop && V.stop(); } catch { } try { j(); } catch { } N.current = null; try { k.terminate(); } catch { } return r.current = null, window.__vpWorker === k && (window.__vpWorker = null), V.reject(new Error("slice canceled")), !0; });
     }
     return r.current;
   }
@@ -1737,21 +1737,45 @@ function _l(e) {
   }
   async function ie(k, F) {
     const ne = (O) => String((O == null ? void 0 : O.message) || O).includes("canceled");
+    const Kt = [], Xt = (V, I, X) => {
+      const ae = ((performance.now() - I) / 1e3).toFixed(1), Ae = String((X == null ? void 0 : X.message) || X);
+      Kt.push(`${V} failed after ${ae}s: ${Ae}`), lt.info(`[vp-retry] ${Kt[Kt.length - 1]}`);
+      try {
+        typeof window < "u" && (window.__vpRungs = Kt.slice(), window.dispatchEvent(new CustomEvent("vp-retry", { detail: { rung: V, seconds: Number(ae), message: Ae, index: Kt.length, total: 3 } })));
+      } catch {
+      }
+    };
+    try {
+      typeof window < "u" && (window.__vpRungs = []);
+    } catch {
+    }
+    const Zt = performance.now();
     try {
       const O = await L(k, JSON.stringify(F));
       return { r: O, economy: !!(O.stats && O.stats.economy) };
     } catch (O) {
       if (ne(O)) throw O;
+      Xt("1/3 full", Zt, O);
       if (F.wall_generator !== "classic") {
         B();
+        const Qt = performance.now();
         try {
           const pe = await L(k, JSON.stringify({ ...F, wall_generator: "classic", keep_stages: !1, reuse_stages: 0 }));
           return { r: pe, economy: !!(pe.stats && pe.stats.economy), classicWalls: !0 };
         } catch (pe) {
           if (ne(pe)) throw pe;
+          Xt("2/3 classic-walls", Qt, pe);
         }
       }
-      return B(), { r: await L(k, JSON.stringify({ ...F, economy: !0, keep_stages: !1, reuse_stages: 0 })), economy: !0, recovered: !0 };
+      B();
+      const en = performance.now();
+      try {
+        return { r: await L(k, JSON.stringify({ ...F, economy: !0, keep_stages: !1, reuse_stages: 0 })), economy: !0, recovered: !0 };
+      } catch (pe) {
+        if (ne(pe)) throw pe;
+        Xt("3/3 economy", en, pe);
+        throw new Error(`${String((pe == null ? void 0 : pe.message) || pe)} — attempts: ${Kt.join(" | ")}`);
+      }
     }
   }
   async function de(k) {
@@ -1763,7 +1787,7 @@ function _l(e) {
     }
   }
   function J(k, F) {
-    k.economy || (k.keep_stages = !0, k.reuse_stages = F && F === w.current ? 2 : 0);
+    k.economy || (k.keep_stages = !(typeof window < "u" && window.__vpNoStageCache), k.reuse_stages = k.keep_stages && F && F === w.current ? 2 : 0);
   }
   function te(k) {
     var Je;
