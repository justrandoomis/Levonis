# Frontend build-time configuration (public values only — never put secrets
# in VITE_* variables; they are compiled into the public JS bundle).

# Google OAuth Web client ID (the same value as GOOGLE_CLIENT_ID in
# wrangler.jsonc). Leave empty to hide/disable Google sign-in.
VITE_GOOGLE_CLIENT_ID=
